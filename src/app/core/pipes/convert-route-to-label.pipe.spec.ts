import { ConvertRouteToLabelPipe } from './convert-route-to-label.pipe';

describe('ConvertRouteToLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertRouteToLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
